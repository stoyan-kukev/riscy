NOTE: File generated by Gemini, because I don't know what I need to do precisely after making the AST, so I needed some kind of roadmap

### Phase 1: Foundation & Data Structures

* [X] **Define the `Type` Representation:** Design how the compiler models types internally (e.g., primitives, pointers, arrays, structs, function signatures).
* [X] **Define the `Symbol` Representation:** Design what a "name" means in your compiler (e.g., is it a variable, a function, or a type? Is it `const` or `var`?).
* [X] **Define the `Scope` Structure:** Create a data structure to hold a map of string names to `Symbol`s, along with a reference to its parent `Scope`.
* [X] **Create the `Analyzer` State:** Build the main structure that will traverse the AST. It should hold the global scope, the "current" scope, and side-tables (hash maps) to link `*Node` addresses to their calculated `Type`s and `Symbol`s.

### Phase 2: Name Binding (Symbol Resolution)

*Walk the AST to map every identifier to a declaration.*

* [ ] **Implement Scope Traversal:** Add logic to push a new scope when entering a block, function, or loop, and pop it when exiting.
* [ ] **Register Declarations:** When visiting a `const`, `var`, `fn`, or type definition, add its name and `Symbol` to the current scope.
* [ ] **Detect Redeclarations:** Throw an error if a user tries to declare a name that already exists in the current scope.
* [ ] **Resolve Identifiers:** When visiting an identifier being *used* in an expression, search the current scope (and parent scopes) to find its `Symbol`.
* [ ] **Detect Undeclared Variables:** Throw an error if an identifier lookup reaches the top of the scope chain without finding a match.

### Phase 3: Type Resolution

*Convert AST type syntax (like `*u32`) into compiler `Type` objects.*

* [ ] **Resolve Primitives:** Map built-in type names (`u8`, `u32`, `bool`, etc.) to their internal `Type` representations.
* [ ] **Resolve Compound Types:** Recursively construct compiler types for pointers, arrays, optional types, and error unions based on their AST prefixes.
* [ ] **Resolve Aggregate Types:** Construct `Type` objects for `struct`, `union`, and `enum` definitions, ensuring all their fields/members are also resolved.
* [ ] **Detect Infinite Types:** Throw an error if a struct contains itself by value (e.g., `struct A { a: A }` instead of `*A`).
* [ ] **Calculate Layouts:** Compute the byte size and alignment requirements for every type (especially structs and unions).

### Phase 4: Type Synthesis & Checking

*Enforce the physics of the language.*

* [ ] **Synthesize Literals:** Assign default types to literals (e.g., `10` -> `int`, `"hello"` -> `[]const u8`, `true` -> `bool`).
* [ ] **Check Variable Initialization:** Ensure the type of the right-hand expression exactly matches the declared type on the left.
* [ ] **Check Assignments:** Ensure the left side of `=` is actually mutable (not `const`), and the right side matches its type.
* [ ] **Check Binary Operators:** - Enforce that `+`, `-`, `*`, `/` require numeric types on both sides.
* Enforce that `and`, `or` require boolean types.
* Enforce that `<<`, `>>`, `&`, `|`, `^` require integers.


* [ ] **Check Unary Operators:** Ensure `-` is only used on numbers, `!` on booleans, etc.
* [ ] **Check Pointer Operations:** Ensure `.*` is only used on pointer types, and `&` (if applicable) correctly wraps a type in a pointer.
* [ ] **Check Field Access (`.`):** Ensure the left side is a struct/union/enum, and the requested field actually exists on that type.
* [ ] **Check Array/Slice Indexing (`[x]`):** Ensure the indexed target is an array or slice, and the index expression is an integer.
* [ ] **Check Function Calls:** Ensure the target is a function, the number of arguments matches the arity, and each argument's type matches the expected parameter type.

### Phase 5: Control Flow Validation

*Verify the logic paths make sense.*

* [ ] **Validate Loop Keywords:** Ensure `break` and `continue` only appear inside `while` or `for` loops.
* [ ] **Check Return Types:** Ensure the expression inside `return expr;` matches the return type of the enclosing function.
* [ ] **Check Exhaustive Returns:** If a function promises to return a value (not `void`), ensure every possible branching path (`if`/`else` or `switch`) ends in a `return`.
* [ ] **(Optional) Detect Unreachable Code:** Flag statements that appear immediately after a `return`, `break`, or `continue` in the same block.
